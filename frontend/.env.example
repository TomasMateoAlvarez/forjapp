# URL base de la API del backend. Si no se define, cae a http://localhost:4000/api.
VITE_API_BASE=http://localhost:4000/api

# Client-Id fijo para esta instalación (multi-tenant simple). Si no se define,
# el frontend genera y persiste uno propio en localStorage la primera vez.
# VITE_CLIENT_ID=default
