# OS
.DS_Store

# Dependencies
node_modules/

# Environment (secretos/config local — usar .env.example como referencia)
.env
.env.local
.env.test

# Backend runtime data (local dev, not source)
backend/seed-demo-data.manifest.json

# Build output / caches
dist/
*.tsbuildinfo

# iOS user-specific Xcode state
ios/**/xcuserdata/
*.xcuserstate

# Claude Code local permission settings (machine-specific)
.claude/settings.local.json
